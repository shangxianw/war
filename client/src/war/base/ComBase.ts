module war
{
	export abstract class ComBase extends DataBase
	{
		public componentId:number;
		protected initAll()
		{
			super.initAll();
		}

		public destroyAll()
		{
			super.destroyAll();
		}
	}
}